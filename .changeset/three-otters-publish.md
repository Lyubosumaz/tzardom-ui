---
'@tzardom-ui/types': patch
'@tzardom-ui/core': patch
'@tzardom-ui/react': patch
---

Add a `LICENSE` and `README.md` to each package, and
`publishConfig.access: "public"` so scoped packages actually publish on a free
npm account. Prep work for the first real release — no runtime behavior changes.
